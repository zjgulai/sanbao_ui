# B24-VIS-W12 · 会话完成与自动化空态的结构校准

日期：2026-09-23

## 范围与来源

这次只复核并收紧两张已有原生观察的核心工作面：

| 原型状态 | 原产品观察 | 本地可复查结构 |
| --- | --- | --- |
| `QDR.M01.reply.completed` | `QB1-12`、`QB1-13` | 三步计划、普通文字确认请求、成功／耗时／模型迭代元信息、复制／评价／从回复分支／更多操作、三条后续建议和任务侧栏。 |
| `QDR.P03.list.empty` | `QB1-01` | 自动化空态、模板、执行记录、模式筛选、排序、保持唤醒关闭和新建入口。 |

来源记录为 `repository-snapshot/docs/research/qoder/qoder-observed-states.md`。两张页面当前没有可在仓库中回读的同状态原产品截图；现有 `figma/previews/QB1-02.png` 只对应新建自动化表单，不能用于自动化空态或会话完成页的像素对照。

## 实际改动

- `src/pages/SessionPage.tsx` 将完成态元信息由“已完成”改为已观察到的“成功”，继续保留耗时和模型迭代信息。
- 同一回复操作将辅助名称从“赞同回复”改为“评价回复”，与 `QB1-13` 的“评价”观察一致；动作仍只写入原型内提示，不访问剪贴板、模型、账户或任何外部服务。
- 自动化空态没有新增猜测性的结构或视觉样式；其现有标签、筛选、排序、保持唤醒和新建入口经本地页面读取与 `QB1-01` 逐项对照后保持不变。

## 本地验收

在 `QDR.M01.reply.completed` 直达页核对：计划标题、三项步骤、文字确认、`成功 · 12s · 1 次模型迭代`、四个回复动作和三条建议均可读取；右侧显示任务回顾、`产出 0`、`来源 0`。

在 `QDR.P03.list.empty&automationView=mine` 直达页核对：页面标题“自动化”，标签“我的自动化／模板／执行记录”，`自动化模式筛选`、`排序`、空态新建按钮和关闭的`保持唤醒（本地模拟）`均可读取。开关初始 `aria-checked="false"`；在本地页点击一次变为 `true`，再次点击恢复 `false`。

再运行 `pnpm build` 和 `python3 output/playwright/batch24/run-today-all-state-routes.py`，确认类型、目录、交互合同与 206 条直达路由没有回归。

## 结论边界

本次仅证明来源文字已明确的结构和本地 fixture 文案。没有同状态原产品图，故不作字体、尺寸、色彩、间距、动画或像素一致结论；Figma、DSH Host、iOS、DMG 和真实自动化执行仍未验收。
